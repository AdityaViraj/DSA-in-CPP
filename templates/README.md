# Templates

Reusable snippets to speed up solving problems.

## Recommended files
- `fast_io.cpp` (fast input/output setup)
- `debug_template.cpp` (optional debug macros)
- `common_typedefs.cpp` (using ll = long long, etc.)

## Notes
- Keep templates minimal.
- Don’t overuse macros in beginner stage.
